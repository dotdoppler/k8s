spring:
  application:
    name: demo-service
app:
  message: "Hello from Local application.yml!"

management:
  endpoint:
    health:
      probes:
        enabled: true
  endpoints:
    web:
      exposure:
        include: health
